# Config file — never commit; use snipemgr.example.yaml as the template
snipemgr.yaml

# Compiled binary
snipemgr
snipemgr.exe

# macOS metadata
.DS_Store

# Local state directory (tracked by state.json, never committed)
.snipemgr/

# Build output
dist/

# Claude Code session kickoff file — never commit
claude-code-kickoff.md
